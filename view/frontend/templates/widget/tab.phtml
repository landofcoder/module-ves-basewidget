<?php
$id = rand().time();
if (isset($tabs)): ?>
<div class="ves-widget widget-tab block <?= $addition_cls; ?> <?php if (isset($stylecls)&&$stylecls) echo 'block-'.$stylecls;?>">
	<?php if (isset($widget_heading)&&!empty($widget_heading)): ?>
	<div class="block-title">
		<h2><span><?= $widget_heading; ?></span></h2>
	</div>
	<?php endif; ?>
	<div class="widget-inner block-content">
		<div id="widget-tabs<?= $id;?>" class="panel-group">

			<ul class="nav nav-tabs" id="widget-tab-label<?= $id;?>">
			  <?php foreach ($tabs as $key => $ac): ?>
			  <li class=""><a href="#tab<?= $id.$key;?>" ><?= $ac['header'];?></a></li>
			  <?php endforeach; ?>
			</ul>

			<div class="tab-content">
			 	<?php foreach ($tabs as $key => $ac): ?>
				  <div class="tab-pane" id="tab<?= $id.$key;?>" style="visibility:visible"><?= $ac['content'];?></div>
			 	<?php endforeach; ?>
	 		</div>

	</div></div>
</div>
<script type="text/javascript">
require(['jquery'],function($){
    $(document).ready(function(){
	require([
		'jquery'
		<?php if($this->helper("Ves\All\Helper\Data")->getConfig("enable_bootstrap_js")): ?>
		,"Ves_All/lib/bootstrap/js/bootstrap.min"
		<?php endif; ?>
		],function($){
			$('#widget-tab-label<?= $id; ?> a').click(function (e) {
				e.preventDefault();
				$(this).tab('show');
			})
			$('#widget-tab-label<?= $id; ?> a:first').tab('show');
		});
	});
});
</script>

<?php endif; ?>
